spring:
  datasource:
    url: jdbc:mysql://localhost:3306/microservices_ratings
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  config:
    import: configserver:http://localhost:8085

  jpa:
    properties:
      hibernate:dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: RATING-SERVICE
server:
  port: 8083

#These are common properties configured in config-server

#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka



